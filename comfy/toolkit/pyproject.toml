[project]
name = "latentdeck-comfy-toolkit"
version = "0.1.0"
description = "Clean-room ComfyUI research toolkit for LatentDeck operators."
requires-python = ">=3.13,<3.14"
license = "Apache-2.0"
license-files = ["LICENSE"]
dependencies = [
    "latentdeck-cartridge==0.1.0",
    "latentdeck-operator-d2==0.1.0",
    "latentdeck-operator-q4==0.1.0",
    "safetensors==0.8.0",
]

[project.optional-dependencies]
cu130 = [
    "torch==2.13.0",
]
test = [
    "pytest==9.1.1",
]

[build-system]
requires = ["uv-build==0.12.7"]
build-backend = "uv_build"

[tool.uv-build]
package-data = [
    "src/latentdeck_comfy_toolkit/operator-descriptor.schema.json",
]

[tool.pytest.ini_options]
addopts = "-ra --strict-markers --import-mode=importlib"
testpaths = ["tests"]

[tool.ruff]
target-version = "py313"
line-length = 100

[tool.ruff.lint]
select = ["B", "E", "F", "I", "SIM", "UP"]
